services:
  web:
    build: .
    ports:
      - "8000:8000"
    labels:
      shipyard.route: "/"
    environment:
      # Optional; list endpoints need credentials when calling AWS.
      AWS_DEFAULT_REGION: ${AWS_DEFAULT_REGION:-us-east-1}
